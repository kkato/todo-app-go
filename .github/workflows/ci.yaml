name: Docker Build and Push

on:
  push:
    branches:
      - main

permissions:
  contents: read
  packages: write

jobs:
  build-and-push:
    uses: kkato/ci/.github/workflows/docker-build-push.yaml@main
    permissions:
      contents: read
      packages: write
